
# SpecsBiz | Smart Business Manager

এটি একটি প্রফেশনাল বিজনেস ম্যানেজমেন্ট ওয়েব অ্যাপ্লিকেশন। নিচে এটি অনলাইনে পাবলিশ করা এবং ভবিষ্যতে আপডেট করার নিয়ম দেওয়া হলো।

## 🌐 কিভাবে অনলাইনে ফ্রিতে পাবলিশ করবেন (Deployment)

আপনার অ্যাপটি আজীবন ফ্রিতে চালানোর জন্য নিচের ধাপগুলো অনুসরণ করুন:

### ধাপ ১: GitHub-এ কোড আপলোড করুন
১. আপনার পিসিতে এই প্রোজেক্টের ফোল্ডারে গিয়ে টার্মিনাল ওপেন করুন এবং নিচের কমান্ডগুলো দিন:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin [আপনার-github-লিঙ্ক]
   git push -u origin main
   ```
*দ্রষ্টব্য: `node_modules` এবং `.next` ফোল্ডার আপলোড হবে না, এটিই সঠিক নিয়ম।*

### ধাপ ২: Vercel-এ হোস্ট করুন
১. [Vercel.com](https://vercel.com/)-এ গিয়ে আপনার GitHub অ্যাকাউন্ট দিয়ে লগইন করুন।
২. **Add New Project**-এ ক্লিক করে আপনার প্রোজেক্টটি **Import** করুন।
৩. কোনো সেটিংস পরিবর্তন না করে **Deploy** বাটনে ক্লিক করুন। ২ মিনিটের মধ্যে আপনার অ্যাপটি লাইভ হয়ে যাবে।

---

## 🔄 ভবিষ্যতে কিভাবে অ্যাপ আপডেট করবেন? (Future Updates)

আপনি যদি ভবিষ্যতে অ্যাপে কোনো পরিবর্তন করেন বা নতুন ফিচার যোগ করেন, তবে লাইভ সাইট আপডেট করা খুব সহজ:

১. আপনার পিসিতে কোড এডিট শেষ করুন।
২. টার্মিনালে নিচের ৩টি কমান্ড দিন:
   ```bash
   git add .
   git commit -m "নতুন ফিচার যোগ করা হয়েছে"
   git push origin main
   ```
৩. **ব্যাস!** আর কিছু করতে হবে না। Vercel অটোমেটিক আপনার এই পরিবর্তনগুলো লাইভ সাইটে আপডেট করে নেবে।

---

## 🛠 প্রযুক্তি (Tech Stack)
- **Framework:** Next.js 15
- **Database:** Firebase Firestore
- **Auth:** Firebase Authentication
- **AI:** Google Genkit (Gemini 2.5 Flash)
- **UI:** Tailwind CSS + ShadCN

Developed by **SpecsXR**.
